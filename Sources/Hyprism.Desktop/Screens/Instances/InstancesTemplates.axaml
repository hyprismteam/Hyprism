<!--
Copyright (C) 2026 Hyprism Launcher
SPDX-License-Identifier: GPL-3.0-only
-->

<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:instances="using:Hyprism.Desktop.Screens.Instances"
                    xmlns:controls="using:Hyprism.Desktop.Controls">
    <DataTemplate x:Key="InstanceVersionTemplate" x:DataType="instances:InstanceVersionItemViewModel">
      <Grid ColumnDefinitions="Auto,*" VerticalAlignment="Center">
        <TextBlock Text="{Binding Version}" />
        <TextBlock Grid.Column="1"
                   Margin="5,0,0,0"
                   Text="{Binding GameVersionLabel}"
                   TextWrapping="NoWrap"
                   TextTrimming="CharacterEllipsis"
                   IsVisible="{Binding HasGameVersionLabel}"
                   Foreground="{DynamicResource TextMutedBrush}" />
      </Grid>
    </DataTemplate>

    <DataTemplate x:Key="InstanceFilterOptionTemplate" x:DataType="instances:InstanceListOptionViewModel">
      <TextBlock Text="{Binding Display}"
                 VerticalAlignment="Center"
                 TextWrapping="NoWrap"
                 TextTrimming="CharacterEllipsis" />
    </DataTemplate>

    <DataTemplate x:Key="ManagedInstanceActionTemplate" x:DataType="instances:InstancesViewModel">
      <Grid Classes="managedActionContent">
        <StackPanel Classes="managedActionIdle"
                    Orientation="Horizontal"
                    Spacing="8"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
          <Path Classes="managedActionIcon"
                IsVisible="{Binding IsManagedInstanceInstalled}"
                Data="{StaticResource PlayIcon}" />
          <Path Classes="managedActionIcon"
                IsVisible="{Binding !IsManagedInstanceInstalled}"
                Data="{StaticResource DownloadIcon}" />
          <TextBlock Text="{Binding ManagedInstanceActionLabel}"
                     VerticalAlignment="Center" />
        </StackPanel>

        <Grid Classes="managedActionProgress"
              ColumnDefinitions="48,*,48"
              VerticalAlignment="Center">
          <Path Classes="managedActionSpinner"
                IsVisible="{Binding !IsManagedInstanceActionRunning}"
                controls:RotatingVisual.IsActive="{Binding ShouldSpinManagedInstanceAction}"
                HorizontalAlignment="Center"
                Data="M10,2 A8,8 0 1 1 2,10" />
          <Path Classes="managedActionIcon running"
                IsVisible="{Binding IsManagedInstanceActionRunning}"
                HorizontalAlignment="Center"
                Data="{StaticResource DirectionsRunIcon}" />
          <controls:FadingTextBlock Grid.Column="1"
                                    Classes="managedActionStatus"
                                    Text="{Binding ManagedInstanceActionStatusText}"
                                    Foreground="{Binding $parent[Button].Foreground}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center" />
          <TextBlock Grid.Column="2"
                     Classes="managedActionMetric"
                     Text="{Binding ManagedInstanceActionMetricText}"
                     Width="48"
                     FontFeatures="tnum"
                     TextAlignment="Right"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Center" />
        </Grid>

        <TextBlock Classes="managedActionCancel"
                   Text="{Binding ManagedInstanceActionCancelLabel}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
      </Grid>
    </DataTemplate>

</ResourceDictionary>
